# План

## Переименование проекта: Harness → fi (в честь Фибоначчи)

Цель: проект называется **fi**; запуск — `pnpm fi web`.

### Уровень 1 — уровень форка (дёшево, 1–2 коммита)

- [x] Скрипт `fi` в корневом `package.json` (алиас CLI), запуск `pnpm fi web` работает.
- [x] `README.md` использует `pnpm fi web`.
- [ ] Переименовать GitHub-репозиторий `harness` → `fi` (GitHub сам сделает редирект старых ссылок).
- [ ] `README.md`: название `Harness` → `fi`, clone-URL `…/mhuruuk/fi.git`, `cd fi`, URL документации `…/fi/…`.
- [ ] Корневые `AGENTS.md`/`CLAUDE.md`: «DeepSeek Harness» → «fi» (по желанию).

### Уровень 2 — CLI `dsh` → `fi` (средне, дни)

- [ ] `bin` в `apps/cli/package.json` и все `pnpm dsh`-вызовы в скриптах, доках, CI.
- [ ] Перезапись снапшотов и тестов, где `dsh` входит в ожидаемый вывод.

### Уровень 3 — scope пакетов `@deepseek-ai/dsh-*` → `@fi/*` (дорого, недели)

- [ ] ~150 пакетов, все workspace-зависимости, конвейер публикации, rescope vendor, npm.

Для личного форка достаточно уровня 1 + текущего алиаса; уровни 2–3 — только при публикации под своим именем.
